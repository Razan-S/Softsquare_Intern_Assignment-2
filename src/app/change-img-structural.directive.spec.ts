import { ChangeImgStructuralDirective } from './show-if-not-null';

describe('ChangeImgStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeImgStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
